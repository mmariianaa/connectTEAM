import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TablerosytareasPage } from './tablerosytareas.page';

describe('TablerosytareasPage', () => {
  let component: TablerosytareasPage;
  let fixture: ComponentFixture<TablerosytareasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TablerosytareasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
